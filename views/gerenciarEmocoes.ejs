<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('partials/favicon') %>
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/menu.css">
    <link rel="stylesheet" href="/css/gerenciarEmocoes.css">
    <%- include('partials/imports') %>
</head>
<body>
    <%- include('partials/menu') %>

    <div class="emocoes-container">
        <div class="emocoes-header">
            <h1>Gerenciamento de Emoções</h1>
            <p>Gerencie as emoções disponíveis para atualizações de leitura</p>
            <button class="btn-nova-emocao" onclick="abrirModalNovaEmocao()">
                <i class="fas fa-plus"></i> Nova Emoção
            </button>
        </div>

        <div class="emocoes-grid" id="emocoes-grid">
            <div class="carregando">Carregando emoções...</div>
        </div>
    </div>

    <!-- Overlay para modal de emoção -->
    <div id="modal-overlay" class="overlay" onclick="fecharModalEmocao()" style="display: none;"></div>
    
    <!-- Modal para nova emoção -->
    <div class="form-container" id="modal-emocao" style="display: none;">
        <form id="form-emocao">
            <input type="hidden" id="emocao-id">
            
            <h2 id="modal-titulo">Nova Emoção</h2>
            
            <p class="form-subtitulo">Nome da Emoção</p>
            <input type="text" id="emocao-nome" placeholder="Nome da emoção" required maxlength="50">

            <p class="form-subtitulo">Emoji</p>
            <input type="text" id="emocao-emoji" placeholder="😊" required maxlength="10">

            <p class="form-subtitulo">Cor</p>
            <input type="color" id="emocao-cor" value="#6c5ce7">

            <p class="form-subtitulo">Status da emoção</p>
            <div class="radio-group">
                <div class="radio-option">
                    <input type="radio" id="emocao-ativa" name="emocao-status" value="true" checked>
                    <label for="emocao-ativa">
                        <i class="fas fa-check"></i>
                        Ativa
                        <span class="option-description">Disponível para seleção</span>
                    </label>
                </div>
                <div class="radio-option">
                    <input type="radio" id="emocao-inativa" name="emocao-status" value="false">
                    <label for="emocao-inativa">
                        <i class="fas fa-times"></i>
                        Inativa
                        <span class="option-description">Não aparece para seleção</span>
                    </label>
                </div>
            </div>

            <div class="form-buttons">
                <button type="submit" class="botao-padrao" id="confirmar-emocao">Salvar</button>
                <button type="button" id="cancelar-emocao" onclick="fecharModalEmocao()">Cancelar</button>
            </div>
        </form>
    </div>

    <script>
        const userId = '<%= typeof userId !== "undefined" && userId ? userId : null %>';
        const userType = '<%= typeof userType !== "undefined" && userType ? userType : null %>';
    </script>
    <script src="/js/gerenciarEmocoes.js"></script>
</body>
</html>
